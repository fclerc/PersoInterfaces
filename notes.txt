chercher les TODO
pour l'interface test stratégie : faire choisir le contexte de séquence dans filechoice. En plus : il ne faut pas mettreles boutons de gestion de fichier. Et en plus : il faut donner le choix pour tous les fichiers (profil, contexte)
toutes les comparaisons avec chaines : toLowerCase
add ids when storing (if necessary)

-dans les explications pour le prof : indiquer quels sont les paramètres obligatoires / optionnels (et éventuellement les générer automatiquement lors de la création d'activité, empêcher leur suppression...)

important : 
-if no 'xmlns' attribute to root of xml, jquery automatically adds it to new elements inserted
-if 'xmlns' attribute on a node of xml doc, then php xpath ignores these nodes in queries.
conclusion : make sure that all xml docs treated by jquery (and where it will insert nodes) have xmlns attribute on their root.
En fait : dans le xpath, utiliser local-name() et tout va bien dans tous les cas.

changer scale sex  :F - M (et pas H)

currently I do no use lower bounds for duration and nbofActivitiues

voir zend

bugs :
-create complex, and edit AND before defining indicator (mais pas si grave, puisqu'on peut supprimer la contrainte complexe après...)


incompréhensible : jquery, pour la première création de l'attribut maxPoints : tout en minuscule.
Pour la suite : prend bien en compte la casse...


pour le rapport:  intro sur le labo, ses chiffres clé, ses missions, principales actions.

voir comment traiter la notion de order et permettre une définition facile (basée sur une liste de contextes disponibles,...)
édition des ressources, en utilisant l'attribut group pour les 'dossiers'

RESOURCES / REMOVE TOUS LES ID ET AJOUTE DE NOUVEAU
modélisation complète

Fichier config
!script  insertion dans xmldisplay

-inputContainer comme argument dans les fonctions scale

-refactoring

idAdderRunner : à utiliser quand on vient de créer un nouveau xml, avec des tags sans id

!sur serveur : toutes url en absolu

Pour activités : nécessité de réécrire un code spécifique pour l'afficher, dans la mesure où on ne veut pas aficher la même chose, et la fonction manipulateXML que j'ai écrite doit rester générique.


For xml documents where jquery might add elements : there are case problems, thus I write everything in lower case. Other solution : use toLowerCase everytime needed (for comparisons)


Useful link for colors : http://web.njit.edu/~kevin/rgb.txt.html#columns









http://www.localeplanet.com/support/howto-localize-js.html

python po2json ../fr.po


Faire facilement le pot : rechercher-remplacer:
(.+)\r
msgid "$1"\rmsgstr ""\r\r

Supprimer valeur de l'attribut:
:.+\r
\r